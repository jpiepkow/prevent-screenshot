{
  "name": "prevent-screenshot",
  "version": "1.0.4",
  "description": "Starting as just deleting screenshots because they are hard to prevent this lib has turned into an autodeleter that listens on a directory/directories and deletes all/files that match a string parse that have been added after the program started running.",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "author": "jordan piepkow",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.17.1",
    "node-watch": "^0.4.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jpiepkow/prevent-screenshot"
  }
}
